pipeline {
  agent any

  stages {
    stage('Checkout') {
      steps {
        echo 'Checkout done'
      }
    }

    stage('Build') {
      steps {
        sh 'echo Build step'
      }
    }

    stage('Test') {
      steps {
        sh 'echo Test step'
      }
    }
  }
}
